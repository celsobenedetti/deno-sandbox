/**
 * Multiplies two numbers together
 */

export function multiply(a: number, b: number) {
  return a * b;
}

if (import.meta.main) {
  // The following command generates documentation for this file based on the JSDocs
  // $ deno doc --html --name="My library" 3-docs-generation.ts
}
